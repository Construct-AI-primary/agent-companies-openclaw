# SQL

**Status**: 📋 Placeholder — OpenClaw-specific SQL queries and data scripts go here.

## Purpose

This directory will contain SQL scripts used for OpenClaw orchestration — data seeding, reporting queries, agent registration queries, reconciliation, and batch operations.

## Expected Contents

- `seed-agents.sql` — Register agents in OpenClaw
- `seed-companies.sql` — Register companies and teams
- `reports/` — Reporting queries
- `reconciliation.sql` — Cross-system reconciliation
- `migrations/` — Data migrations

## Reference

Paperclip SQL scripts (for reference): `agent-companies-paperclip/scripts/sql/` and root SQL files in `agent-companies-paperclip/`.

---

*Fill this in when OpenClaw data model is understood.*